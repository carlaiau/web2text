import ch.ethz.dalab.web2text.utilities.Util
import ch.ethz.dalab.web2text.ApplyLabelsToPage._

object CarlsCreateClean {

    def main(args: Array[String]): Unit = {
        /*
        
        val docs = getListOfFiles("/home/caiau/School/processing/individuals/a")

        var counter = 0
        for(doc <- docs.slice(0,500)){
            val fileName = doc.toString().split("/").last
            val labels = Util.loadFile("/home/caiau/School/data/web2textRelated/labels/fileName").split(",").map(_.toInt)
            applyLabelsToPage(doc, labels, "/home/caiau/School/data/web2textRelated/cleaned/" + fileName)

        */
        // val docs = getListOfFiles("/home/caiau/School/processing/individuals/a")
        //val labels = Util.loadFile("/home/caiau/School/web2text/src/main/python/labels_output").split(",").map(_.toInt)
        //applyLabelsToPage("/home/caiau/School/web2text/samples/a.xml", labels, "/home/caiau/School/web2text/output/clean/a.txt")
    }
}